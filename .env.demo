# Standalone demo deployment (Vercel), built with: vite build --mode demo.
# The whole product runs on the in-memory mock data layer, so every screen
# and role is reachable for review.
VITE_ENABLE_ROLE_PREVIEW=true
