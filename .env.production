# Production expects a real API and Entra ID. The demo deployment on Vercel
# overrides these through project environment variables (mocks on, msal off)
# until the backend exists.
VITE_API_BASE_URL=/api
VITE_ENABLE_MOCKS=false
VITE_AUTH_MODE=msal
VITE_ENABLE_ROLE_PREVIEW=false
